# GeoFencingTest
iOS geofencing SDK (SwiftPM) using CoreLocation circular regions. Delivers enter/exit events to your app via NotificationCenter and can best‑effort POST JSON events to a webhook. Works with background relaunch (iOS limits apply: 20 regions, no relaunch after force-quit).
